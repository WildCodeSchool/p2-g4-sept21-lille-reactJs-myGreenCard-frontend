import styled from 'styled-components';

const SHome = styled.main`
  button {
    &.dark {
      background: white;
      span {
        background: black;
        left: 38px;
      }
    }
  }
`;

export default SHome;
