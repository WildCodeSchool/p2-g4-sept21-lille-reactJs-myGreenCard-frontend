import styled from 'styled-components';

const SLoggingPage = styled.main`
  button {
    &.dark {
      background: white;
      span {
        background: black;
        left: 38px;
      }
    }
  }
`;

export default SLoggingPage;
